[gd_resource type="Resource" script_class="SystemDef" format=3]

[ext_resource type="Script" path="res://scripts/schema/system_def.gd" id="1_hold"]

[resource]
script = ExtResource("1_hold")
tags = PackedStringArray("cargo")
cargo_capacity = 40.0
display_name = "False-Bottom Hold"
description = "A smuggler's hold with room the manifest never mentions — twice the strapdown pod's space, and just as quiet. Sits between a starter's pockets and a freighter's belly."
grade = 2
mark = 1
mass = 7.0
affix_ids = PackedStringArray()
