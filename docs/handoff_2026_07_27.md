# Session handoff — 2026-07-26/27

**Read this first if you are picking up after the long session.** Branch
`the-legend`, 58 commits over two days, working tree clean, 38/43 suites green
(the 5 "failures" are a runner artefact — see *Testing* below).

---

## 1. THE GAME IS NOW "RISE OF ADÉN"

Renamed from *Bleakflame*. **Adén** is the Wardens' word for idiots — `tu` = the,
`adé` = idiot (sing.), `adén` = idiots (pl.), so **Tu Adén** = The Idiots
(supersedes ADO'VIETES). The title states the Saga's ending on the box and spoils
nothing, because the player cannot read it until the late reveal teaches them.

> **THE SAVE DIRECTORY IS STILL `Bleakflame` AND MUST STAY THAT WAY.**
> Godot derives `user://` from `config/name`, so the rename pins BOTH
> `application/config/use_custom_user_dir=true` and
> `config/custom_user_dir_name="Godot/app_userdata/Bleakflame"` in project.godot.
> **Both lines are required** — `custom_user_dir_name` is ignored without the flag,
> and with the flag on Godot drops the `Godot/app_userdata/` prefix it normally
> adds, so the full relative path has to be spelled out. The first attempt at this
> silently pointed at `app_userdata/Rise of Adén/` and found no save. Caught by
> running the game, not by reading the code.

`assets/ships/future/bleakflame_legendary` is no longer the titular ship. It is
still the endgame prize; open question whether it earns its name in-fiction or
takes a new one.

---

## 2. What shipped

**Systems.** Gear levels derived from grade+mark (green at 3, blue at 5, purple at
15) with level as a real equip requirement; 8 new items filling the L1–2 hole the
gate opened (incl. the Sputter Screen — without it a new pilot had *no shield at
any price*); the starter Rooster re-fitted to white/grey so the first upgrade is an
upgrade. XP centralised in `scripts/xp.gd` — both halves, curve and rewards — with
a KNEE at level 5 so the late game can be slowed without touching a low-level pace
the user has now validated by play.

**World.** Pirate postings with level bands (difficulty as a property of the map);
authored `Tactic.STRAFE` so tuning speed can no longer silently rewrite behaviour;
six Long Lane convoys seeded along the road; V-Shrike acquire at full sensor reach
(a 950u cap on a 91,000u road made interception near-impossible); ships below 30%
hull lose a quarter of top speed, below 10% half.

**Story.** The Saga is **The Rise**, with six movements recorded in
`docs/the_convergence.md` (The Gate · The Light · The Choir · The Key · The Dark ·
epilogue The Idiot). The Cinder Reach Campaign is **The Legend**. `requires_level`
and `requires_days` gate beats; `Quests.pending_reason()` explains a cold trail so
silence never reads as breakage; the log lists started-but-resting spines.

**Armor spec** reworked to DR-only with ablative as an opt-in type (+10% fresh →
−5% spent, crossover at 33% integrity). Spec only; not built.

---

## 3. Bugs fixed (18, all verified before fixing)

Cloak permanent invisibility · wanted pilots taking 2× gravity and 220 crash damage
· chips bypassing the level gate · a ground **soft-lock** (dying while meditating
left you rooted forever) · Esc closing a panel also ejecting you from the room ·
freed prey freezing pirates permanently · specialists healing the *wanted* player ·
Cinderweb's sanctuary drifted to 1800 vs everyone else's 2600 **and** measured from
the world origin · Nemesis grudges and `Pilot.met` surviving New Game · Xenology
charting the four ephemeral quest markers · radar dividing by zero with no sensor ·
gate console reading past its array at the Saga's climax · Verge purchases vanishing
into the station stash · Verge turn-ins skipping `check_new_work` · `%d/%d` printing
raw in the campaign banner · the wipe button living inside the "your stash is safe"
column · Vyper installing level-gated gear.

---

## 4. STILL OPEN — verified, not fixed

**Live bugs:**
- **`ship.gd:1663` `_run_repair` does not dedupe the two ally groups** — Science
  Repair Field heals every Guardian and Trader at **2×**. Same bug as the planetoid
  one fixed today, one file over. `_engage_bulwark` (`:1003`) over-reports its count
  for the same reason.
- **`ShoalPad.try_dock` overrides wholesale** and loses the size gate (an oversized
  hull can berth at the Shoal), the fault tiers, and `SaveGame.docking_taught`.
- **`epharon_town.gd:1311`** silently **destroys** a ground item when the hold is
  full — the only one of 7 "give the player this" copies that does.
- **`prospect_deck.gd:207`** still calls `MissionLog.accept` raw, losing the
  "log full" message.
- **`_flash(str(r.msg))` sits inside `if r.ok`** at `dock_screen.gd:3175` and
  `prospect_deck.gd:225`, so a *refused* turn-in is silent at both.

**Design decisions, not mechanical fixes:**
- **Boot auto-docks** (`flight_test.gd:144`), advancing a game day every launch —
  farms Insight and skips the `requires_days` waits. Suppress on the boot dock, or
  stop auto-docking?
- **Scan Data has no per-target gate** — park by the station, press [1] every 3.2s,
  free income forever.

**Dead code**, all grep-proven and safe to delete: `spawn_escort`, `Sfx.play_vo`,
`callsign_vo`, the HUD veil subsystem (2 always-allocated ColorRects), `_toggle_array`
(which makes the "ordnance cold" pip permanently dead), `_bar_pending_talk` +
`_present_bar_talk_if_shown`, `_note_open_tab` (whose comment claims `refresh()`
calls it — it does not), `_populate_component_hold`, `_on_list_selected`,
`_material_glyph/_color`, `_arm_memorize_lesson`, `can_make_peace`, `affix_text`,
`COMM_TERMINAL_ALT`, `is_dark`, `Tutor.step_for`, and the unsaved Prospector Scanner
still built in the generator.

**Good news:** a reviewer regenerated all 53 `.tres` into an isolated project and
byte-compared — **zero generator drift**. That hazard has bitten twice; the
`_save_hull` guards are holding.

---

## 5. Next up

**`docs/component_refactor.md` is the plan of record.** The user's architecture:
one `DockTab` type plus composable panels, *"so fixing it in one place fixes it
everywhere."* The audit proved the thesis — 30 clear-then-refill sites (24 unsafe),
16 hand-rolled Esc captures, 11 spawn sites in 4 orders, 4 talk-drain copies. Do the
cross-cutting components **before** the dock split: they fix live bugs, the split
fixes structure.

**The Legend**: beats 1–2 built and playable. Beat 3 ("who is asking") is next and is
mostly writing. Open question the user raised: **does Odessa come to Percival, or is
it exactly where she cannot go?** Beat 5 is four verbs wearing one number; seven beats
almost certainly is not enough.

---

## 6. Testing — read before trusting a green run

- **The 5 "failures" are a scratch-runner artefact**, not real. `test_chat` prints
  "OK", `test_affixes` "all passed", and the runner only recognises "ALL PASS";
  `test_gate` and `test_ground_notice` are cut off by its 600-frame cap. All five
  pass when run individually. Fix the runner rather than eyeballing past it.
- **ALWAYS pass `--quit-after N` to scene tests.** A script that fails to compile
  hangs forever with **empty output**, and two orphaned Godot processes were left
  burning 1.6GB this session because of exactly that.
- **A new `class_name` needs `--import`** before anything can reference it.
- **SABOTAGE-VERIFY every new assertion.** Three tests this session agreed with the
  bug they were written alongside, because assertion and code came from the same
  wrong idea. A test that cannot fail is not a test.
- **Screenshots find what tests cannot.** The `%d/%d` banner bug shipped since the
  initial commit and survived 660 dock assertions — it is invisible in source
  because both call sites look correct in isolation. Drive the real scene and grab
  `get_viewport().get_texture().get_image().save_png()` (what F12 does).

> **CLAUDE.md is gitignored** (`.gitignore:30`) — no history, no recovery. Six stale
> entries were corrected this session (KILL_XP location, sanctuary 1800→2600,
> Cinderweb 4500→6000 HP, `[Z]` ordnance→`[R]`, T/Y targeting→LMB/TAB,
> `spawn_escort`→`spawn_shadow_escort`). It steers every session, so its drift
> compounds faster than a code leftover's.
